// android/build.gradle

buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.1' // Utilisez une version existante et stable
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.0" // Version Kotlin stable
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
